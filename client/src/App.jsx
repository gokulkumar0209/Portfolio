import { useState } from "react";

function App() {
	const [count, setCount] = useState(0);

	return (
		<>
			<head>
				<title>YYY</title>
			</head>
			<body className="bg-black min-h-screen">
				<header className="bg-gray-800 text-white font-bold">G</header>
			</body>
		</>
	);
}

export default App;
